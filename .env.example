# ============================================
# OPTIONAL: All variables have defaults
# The service works without this file
# ============================================

# Server (defaults: PORT=3000, HOST=0.0.0.0)
# PORT=3000
# HOST=0.0.0.0

# Cache (default: 300 seconds)
# CACHE_TTL_SECONDS=300

# BVC API (defaults: production URLs)
# BVC_REST_API_URL=https://rest.bvc.com.co
# BVC_WEB_URL=https://www.bvc.com.co

# Token (optional: if provided, avoids using Playwright)
# BVC_TOKEN=your-token-here

# Playwright (defaults: enabled=true, headless=true, timeout=30000ms)
# ENABLE_PLAYWRIGHT=true
# PLAYWRIGHT_HEADLESS=true
# PLAYWRIGHT_TIMEOUT_MS=30000

# Logging (default: info)
# LOG_LEVEL=info
